name: Exécution des jobs cron Kërjom

on:
  schedule:
    # Toutes les heures (00:00, 01:00, 02:00...)
    - cron: '0 * * * *'
  workflow_dispatch:  # Pour exécuter manuellement si besoin

jobs:
  run-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Appeler la fonction Edge Supabase
        run: |

          - name: Debug secret (masqué)
  run: |
    if [ -n "${{ secrets.SUPABASE_URL }}" ]; then
      echo "SUPABASE_URL is set (value hidden)"
    else
      echo "SUPABASE_URL is EMPTY"
    fi
          
          curl -X POST "${{ secrets.SUPABASE_URL }}/functions/v1/run-scheduled-jobs" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json"
